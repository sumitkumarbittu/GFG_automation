{
  "name": "geeksforgeeks-traversal-lab",
  "version": "1.1.0",
  "private": true,
  "type": "commonjs",
  "description": "User-controlled synthetic editor traversal lab for GeeksforGeeks practice pages",
  "scripts": {
    "test": "node --test",
    "check": "find src test -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
